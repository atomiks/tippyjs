---
title: Demo
path: /
index: 0
---

import Tippy from '../components/Tippy'
import { Button, Demo, Link, Row, Col } from '../components/Framework'
import { ALL_PLACEMENTS, EXTRA_ANIMATIONS } from '../utils'
import Dropdown from '../components/examples/Dropdown'
import Singleton from '../components/examples/Singleton'
import Nesting from '../components/examples/Nesting'
import Multiple from '../components/examples/Multiple'

Tippy.js is a highly customizable tooltip and popover library powered by
Popper.js. You can use Tippy.js to create simple static text tooltips, or
complex interactive HTML popovers that utilize AJAX.

Its purpose is to provide a complete set of features when dealing with a range
of different "popper" elements such as tooltips, popovers, dropdowns, and more,
while handling the basic structure and customization of these elements for you.
The API is designed to be simple and easy to use with sensible defaults while
also giving you full control where needed.

It's compatible with IE11+, about 99% of desktop users and 97% of mobile users.
Tippy.js also works very well on touch devices, and handles edge cases related
to hybrid devices and iOS that you may have encountered in other libraries.

---

### Exploring Tippy's features

Given an element on the document...

```html
<button id="myButton">My Button</button>
```

We can give it many different types of tippy tooltips. Let's explore some of
them below!

### Default

The default tippy tooltip looks like this. It has a nifty background fill
animation!

<Demo>
  <Tippy>
    <Button>My Button</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  content: "I'm a Tippy tooltip!",
})
```

By default, a tippy is triggered by either `mouseenter` or `focus` events so
they appear when hovered, focused via keyboard navigation, or tapped when using
a touch device.

---

### Placement

Tooltips can be placed in four base ways in relation to the reference element.
Additionally, the tooltip can be shifted along the axis using the suffix
`-start` or `-end`.

<Demo>
  <Row>
    {ALL_PLACEMENTS.map((placement, i) => (
      <Col key={placement} base={6} md={4} lg={6} xl={4}>
        <Tippy
          key={placement}
          content="Tooltip"
          placement={placement}
          flip={false}
        >
          <Button
            style={{
              display: 'inline-block',
              width: '100%',
              height: 50,
              margin: '10px 0',
            }}
          >
            {placement}
          </Button>
        </Tippy>
      </Col>
    ))}
  </Row>
</Demo>

```js
tippy('#myButton', {
  placement: 'bottom',
})
```

If a tippy cannot fit within its desired placement, it will flip to the opposite
placement if there is not enough space. In the above examples, flipping has been
disabled to demonstrate each placement properly.

---

### Arrows

Tooltips can have an arrow that points toward the reference element. The size
and proportion can also be modified with CSS. Getting even more advanced, you
can use any type of SVG shape as the arrow, like a speech bubble shape.

<Demo>
  <Tippy arrow animation="fade">
    <Button>Default</Button>
  </Tippy>
  <Tippy arrow animation="fade" arrowType="round" animation="fade">
    <Button>Round</Button>
  </Tippy>
  <Tippy arrow animation="fade" theme="large-arrow" distance={16}>
    <Button>Large</Button>
  </Tippy>
  <Tippy arrow animation="fade" theme="small-arrow" distance={8}>
    <Button>Small</Button>
  </Tippy>
  <Tippy arrow animation="fade" theme="wide-arrow">
    <Button>Wide</Button>
  </Tippy>
  <Tippy arrow animation="fade" theme="narrow-arrow">
    <Button>Narrow</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  arrow: true,
  arrowType: 'round', // omit for the default sharp arrow
})
```

---

### Animations

Tooltips can have different types of transition animations. Note, the filling
effect has been disabled in these examples (`animateFill: false`).

#### Default animations

These animations are included in the main default CSS file.

<Demo>
  {['fade', 'shift-away'].map(animation => (
    <Tippy key={animation} animation={animation} animateFill={false}>
      <Button>{animation}</Button>
    </Tippy>
  ))}
</Demo>

```js
tippy('#myButton', {
  animateFill: false,
  animation: 'fade',
})
```

#### Extra included animations

These animations are included in the package and can be imported separately.

<Demo>
  <Row>
    {EXTRA_ANIMATIONS.map(animation => (
      <Col key={animation} base={12} xs={6} md={4} lg={6} xl={4}>
        <Tippy animation={animation} animateFill={false}>
          <Button
            style={{
              width: '100%',
              margin: '10px 0',
              height: 50,
            }}
          >
            {animation}
          </Button>
        </Tippy>
      </Col>
    ))}
  </Row>
</Demo>

#### Inertia / slingshot elastic effect

The CSS transition-timing-function can be modified, by default it's subtle and
looks like this:

<Demo>
  {EXTRA_ANIMATIONS.filter(animation => animation.includes('scale')).map(
    animation => (
      <Tippy
        key={animation}
        animation={animation}
        animateFill={false}
        inertia={true}
        duration={[450, 175]}
      >
        <Button>{animation}</Button>
      </Tippy>
    ),
  )}
</Demo>

#### CSS keyframe animations

Getting more advanced, you can use actual CSS animations (`@keyframes` rules),
for example using the `animate.css` package:

<Demo>
  <Tippy
    animation="fade"
    animateFill={false}
    duration={[500, 200]}
    onMount={instance => {
      requestAnimationFrame(() => {
        instance.popperChildren.tooltip.classList.add('rubberBand', 'animated')
      })
    }}
    onHidden={instance => {
      instance.popperChildren.tooltip.classList.remove('rubberBand', 'animated')
    }}
  >
    <Button>rubberBand</Button>
  </Tippy>
  <Tippy
    animation="fade"
    animateFill={false}
    duration={[500, 200]}
    onMount={instance => {
      requestAnimationFrame(() => {
        instance.popperChildren.tooltip.classList.add('tada', 'animated')
      })
    }}
    onHidden={instance => {
      instance.popperChildren.tooltip.classList.remove('tada', 'animated')
    }}
  >
    <Button>tada</Button>
  </Tippy>
</Demo>

#### JavaScript spring animations

Animation libraries like `animejs` can also be integrated.

---

### Themes

Tooltips can have custom styling.

#### Default theme

The dark theme is default (`#333` background color):

<Demo>
  <Tippy animateFill={false}>
    <Button>Default</Button>
  </Tippy>
</Demo>

#### Included themes

These themes are included in the package and can be imported separately.

<Demo>
  {['light', 'light-border', 'material', 'translucent'].map(theme => (
    <Tippy key={theme} theme={theme} animateFill={false}>
      <Button>{theme}</Button>
    </Tippy>
  ))}
</Demo>

```js
tippy('#myButton', {
  theme: 'tippy-light',
})
```

#### Custom themes

You can apply any CSS to a tippy via a theme.

<Demo>
  {['gradient', 'retro', 'forest'].map(theme => (
    <Tippy key={theme} theme={theme} animateFill={false}>
      <Button>{theme}</Button>
    </Tippy>
  ))}
</Demo>

---

### Triggers

Tooltips can also be triggered by `click` or `focus` events.

<Demo>
  <Tippy trigger="click">
    <Button>Click</Button>
  </Tippy>
  <Tippy trigger="focus" hideOnClick={false}>
    <Button>Focus</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  trigger: 'click', // or 'focus'
})
```

---

### Interactivity

Tooltips can be interactive, allowing you to hover over and click inside them.

<Demo>
  <Tippy interactive content="You can select the text inside here.">
    <Button>Interactive</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  content: 'You can select the text inside here.',
  interactive: true,
})
```

---

### HTML Content

Tooltips can contain HTML.

<Demo>
  <Tippy
    content={
      <strong>
        Bolded <span style={{ color: 'aqua' }}>content</span>
      </strong>
    }
  >
    <Button>HTML Content</Button>
  </Tippy>
  <Dropdown />
</Demo>

```js
tippy('#myButton', {
  content: '<strong>Bolded <span style="color: aqua;">content</span></strong>',
})
```

---

### Duration

Tooltips can have different animation durations.

<Demo>
  <Tippy duration={0}>
    <Button>0</Button>
  </Tippy>
  <Tippy duration={1000}>
    <Button>1000</Button>
  </Tippy>
  <Tippy duration={[800, 100]}>
    <Button>[800, 100]</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  duration: 0,
})
```

---

### Delay

Tooltips can delay hiding or showing after a trigger.

<Demo>
  <Tippy delay={500}>
    <Button>500</Button>
  </Tippy>
  <Tippy delay={[800, 0]}>
    <Button>[800, 0]</Button>
  </Tippy>
  <Tippy delay={[0, 800]}>
    <Button>[0, 800]</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  delay: 500,
})
```

---

### Follow Cursor

Tooltips can follow the mouse cursor and abide by a certain axis. Additionally,
the tooltip can follow the cursor until it shows, at which point it will stop
following (initial).

<Demo>
  <Tippy followCursor animateFill={false} duration={200} animation="fade">
    <Button>Default</Button>
  </Tippy>
  <Tippy
    followCursor="horizontal"
    animateFill={false}
    duration={200}
    animation="fade"
  >
    <Button>Horizontal</Button>
  </Tippy>
  <Tippy
    followCursor="vertical"
    animateFill={false}
    duration={200}
    animation="fade"
  >
    <Button>Vertical</Button>
  </Tippy>
  <Tippy
    followCursor="initial"
    animateFill={false}
    delay={200}
    duration={200}
    animation="fade"
  >
    <Button>Initial</Button>
  </Tippy>
</Demo>

```js
tippy('#myButton', {
  followCursor: true,
})
```

---

### SVGs

Tooltips can be placed on SVG nodes, where `followCursor: 'initial'` becomes
very useful, since it can be placed directly on the line.

<svg height="150" width="150">
  <Tippy followCursor="initial" animation="fade" delay={100} arrow>
    <line
      x1="0"
      y1="0"
      x2="150"
      y2="150"
      style={{ stroke: 'tomato', strokeWidth: 5 }}
    />
  </Tippy>
</svg>

```js
tippy('line', {
  followCursor: 'initial',
  delay: 200,
  arrow: true,
})
```

---

### Singleton

Use a single tooltip for many different reference elements. This allows you to
"group" tooltips with a shared timer to improve UX when elements near each other
have tooltips with a `delay` prop.

Non-singleton tippy with `delay: 500`:

<Demo>
  <Singleton />
</Demo>

Singleton tippy to group each tippy's `delay: 500`:

<Demo>
  <Singleton group />
</Demo>

Singleton tippy with a transition:

<Demo>
  <Singleton group transition />
</Demo>

```js
import { createSingleton } from 'tippy.js/addons'

const instances = []
const buttons = document.querySelectorAll('button')

for (let i = 0; i < buttons.length; i++) {
  instances.push(
    tippy(buttons[i], {
      content: `Tooltip ${i + 1}`,
      updateDuration: 400,
    }),
  )
}

createSingleton(instances, { delay: [300, 600] })
```

---

### Nesting

Tippys can be nested within other ones.

<Demo>
  <Nesting />
</Demo>

```js
const content = document.createElement('div')

for (let i = 0; i < 3; i++) {
  const button = document.createElement('button')
  button.textContent = 'Text'
  content.appendChild(button)

  tippy(button, {
    content: "I'm a Tippy tooltip!",
    arrow: true,
    animation: 'fade',
  })
}

tippy('#myButton', {
  content,
  arrow: true,
  theme: 'light-border',
})

// Note: Some extra logic is required to handle some other edge cases
```

---

### Multiple

Attach many tippys to a single element.

<Demo>
  <Multiple />
</Demo>

```js
const placements = ['top', 'bottom', 'left', 'right']

placements
  .reduce(
    (acc, basePlacement) =>
      acc.concat(
        basePlacement,
        `${basePlacement}-start`,
        `${basePlacement}-end`,
      ),
    [],
  )
  .forEach(placement => {
    tippy('#square', {
      content: placement,
      placement,
      multiple: true,
    })
  })
```

<br />

The above is not a complete list of features. There are plenty more!
