import t from 'popper.js'
function e() {
  return (e =
    Object.assign ||
    function(t) {
      for (var e = 1; e < arguments.length; e++) {
        var n = arguments[e]
        for (var i in n)
          Object.prototype.hasOwnProperty.call(n, i) && (t[i] = n[i])
      }
      return t
    }).apply(this, arguments)
}
var n = 'undefined' != typeof window && 'undefined' != typeof document,
  i = n ? navigator.userAgent : '',
  r = /MSIE |Trident\//.test(i),
  o = /UCBrowser\//.test(i),
  a = n && /iPhone|iPad|iPod/.test(navigator.platform),
  s = {
    allowHTML: !0,
    animateFill: !0,
    animation: 'shift-away',
    appendTo: function() {
      return document.body
    },
    aria: 'describedby',
    arrow: !1,
    arrowType: 'sharp',
    boundary: 'scrollParent',
    content: '',
    delay: 0,
    distance: 10,
    duration: [325, 275],
    flip: !0,
    flipBehavior: 'flip',
    flipOnUpdate: !1,
    followCursor: !1,
    hideOnClick: !0,
    ignoreAttributes: !1,
    inertia: !1,
    interactive: !1,
    interactiveBorder: 2,
    interactiveDebounce: 0,
    lazy: !0,
    maxWidth: 350,
    multiple: !1,
    offset: 0,
    onCreate: function() {},
    onHidden: function() {},
    onHide: function() {},
    onMount: function() {},
    onShow: function() {},
    onShown: function() {},
    onTrigger: function() {},
    onUntrigger: function() {},
    placement: 'top',
    popperOptions: {},
    role: 'tooltip',
    showOnInit: !1,
    size: 'regular',
    sticky: !1,
    theme: 'tippy-dark',
    touch: !0,
    touchHold: !1,
    trigger: 'mouseenter focus',
    triggerTarget: null,
    updateDuration: 0,
    wait: null,
    zIndex: 9999,
  },
  p = [
    'arrow',
    'arrowType',
    'boundary',
    'distance',
    'flip',
    'flipBehavior',
    'flipOnUpdate',
    'offset',
    'placement',
    'popperOptions',
  ]
function u(t) {
  return [].slice.call(t)
}
function c(t, e) {
  for (; t; ) {
    if (e(t)) return t
    t = t.parentElement
  }
  return null
}
var d = { passive: !0 },
  l = 4,
  f = 'tippy-iOS',
  m = 'tippy-popper',
  v = 'tippy-tooltip',
  h = 'tippy-content',
  b = 'tippy-backdrop',
  y = 'tippy-arrow',
  g = 'tippy-svgArrow',
  w = '.'.concat(m),
  A = '.'.concat(v),
  T = '.'.concat(h),
  E = '.'.concat(b),
  C = '.'.concat(y),
  I = '.'.concat(g),
  L = { isTouch: !1 },
  O = 0
function k() {
  L.isTouch ||
    ((L.isTouch = !0),
    a && document.body.classList.add(f),
    window.performance && document.addEventListener('mousemove', D))
}
function D() {
  var t = performance.now()
  t - O < 20 &&
    ((L.isTouch = !1),
    document.removeEventListener('mousemove', D),
    a || document.body.classList.remove(f)),
    (O = t)
}
function M() {
  var t = document.activeElement,
    e = t._tippy
  t && t.blur && e && !e.state.isVisible && t.blur()
}
var S = Object.keys(s)
function x(t, e) {
  return {}.hasOwnProperty.call(t, e)
}
function V(t, e, n) {
  if (Array.isArray(t)) {
    var i = t[e]
    return null == i ? n : i
  }
  return t
}
function H(t, e) {
  return t && t.modifiers && t.modifiers[e]
}
function N(t, e) {
  return t.indexOf(e) > -1
}
function U(t) {
  return t instanceof Element
}
function q(t, e) {
  return 'function' == typeof t ? t.apply(null, e) : t
}
function F(t, e) {
  t.filter(function(t) {
    return 'flip' === t.name
  })[0].enabled = e
}
function z() {
  return document.createElement('div')
}
function B(t, e) {
  t.forEach(function(t) {
    t && (t.style.transitionDuration = ''.concat(e, 'ms'))
  })
}
function P(t, e) {
  t.forEach(function(t) {
    t && t.setAttribute('data-state', e)
  })
}
function _(t, n) {
  var i = e(
    {},
    n,
    { content: q(n.content, [t]) },
    n.ignoreAttributes
      ? {}
      : (function(t) {
          return S.reduce(function(e, n) {
            var i = (t.getAttribute('data-tippy-'.concat(n)) || '').trim()
            if (!i) return e
            if ('content' === n) e[n] = i
            else
              try {
                e[n] = JSON.parse(i)
              } catch (t) {
                e[n] = i
              }
            return e
          }, {})
        })(t),
  )
  return (i.arrow || o) && (i.animateFill = !1), i
}
function j(t, e) {
  return 0 === e
    ? t
    : function(i) {
        clearTimeout(n),
          (n = setTimeout(function() {
            t(i)
          }, e))
      }
  var n
}
function W(t, e) {
  t.innerHTML = U(e) ? e.innerHTML : e
}
function R(t, e) {
  if (U(e.content)) W(t, ''), t.appendChild(e.content)
  else if ('function' != typeof e.content) {
    t[e.allowHTML ? 'innerHTML' : 'textContent'] = e.content
  }
}
function X(t) {
  return {
    tooltip: t.querySelector(A),
    backdrop: t.querySelector(E),
    content: t.querySelector(T),
    arrow: t.querySelector(C) || t.querySelector(I),
  }
}
function Y(t) {
  t.setAttribute('data-inertia', '')
}
function J(t) {
  var e = z()
  return (
    'sharp' !== t
      ? ((e.className = g),
        W(
          e,
          'round' === t
            ? '<svg viewBox="0 0 18 7" xmlns="http://www.w3.org/2000/svg"><path d="M0 7s2.021-.015 5.253-4.218C6.584 1.051 7.797.007 9 0c1.203-.007 2.416 1.035 3.761 2.782C16.012 7.005 18 7 18 7H0z"/></svg>'
            : t,
        ))
      : (e.className = y),
    e
  )
}
function G(t) {
  var e = z()
  return (
    (e.className = b), e.setAttribute('data-state', t ? 'visible' : 'hidden'), e
  )
}
function K(t) {
  t.setAttribute('data-interactive', '')
}
function Q(t, e, n) {
  var i =
    o && void 0 !== document.body.style.webkitTransition
      ? 'webkitTransitionEnd'
      : 'transitionend'
  t[e + 'EventListener'](i, n)
}
function Z(t) {
  return t.split('-')[0]
}
function $(t, e, n) {
  n.split(' ').forEach(function(n) {
    t.classList[e](n + '-theme')
  })
}
function tt(t, e) {
  var n = z()
  n.className = m
  var i = z()
  ;(i.className = v),
    (i.id = 'tippy-'.concat(t)),
    i.setAttribute('data-state', 'hidden'),
    i.setAttribute('tabindex', '-1'),
    $(i, 'add', e.theme)
  var r = z()
  return (
    (r.className = h),
    r.setAttribute('data-state', 'hidden'),
    e.interactive && K(i),
    e.arrow &&
      (i.setAttribute('data-arrow', ''), i.appendChild(J(e.arrowType))),
    e.animateFill &&
      (i.appendChild(G(!1)), i.setAttribute('data-animatefill', '')),
    e.inertia && Y(i),
    R(r, e),
    i.appendChild(r),
    n.appendChild(i),
    et(n, e, e, !1),
    n
  )
}
function et(t, e, n, i) {
  var r = X(t),
    o = r.tooltip,
    a = r.content,
    s = r.backdrop,
    p = r.arrow
  ;(t.style.zIndex = '' + n.zIndex),
    o.setAttribute('data-size', n.size),
    o.setAttribute('data-animation', n.animation),
    (o.style.maxWidth =
      n.maxWidth + ('number' == typeof n.maxWidth ? 'px' : '')),
    n.role ? o.setAttribute('role', n.role) : o.removeAttribute('role'),
    e.content !== n.content && R(a, n),
    !e.animateFill && n.animateFill
      ? (o.appendChild(G(i)), o.setAttribute('data-animatefill', ''))
      : e.animateFill &&
        !n.animateFill &&
        (o.removeChild(s), o.removeAttribute('data-animatefill')),
    !e.arrow && n.arrow
      ? (o.appendChild(J(n.arrowType)), o.setAttribute('data-arrow', ''))
      : e.arrow &&
        !n.arrow &&
        (o.removeChild(p), o.removeAttribute('data-arrow')),
    e.arrow &&
      n.arrow &&
      e.arrowType !== n.arrowType &&
      o.replaceChild(J(n.arrowType), p),
    !e.interactive && n.interactive
      ? K(o)
      : e.interactive &&
        !n.interactive &&
        (function(t) {
          t.removeAttribute('data-interactive')
        })(o),
    !e.inertia && n.inertia
      ? Y(o)
      : e.inertia &&
        !n.inertia &&
        (function(t) {
          t.removeAttribute('data-inertia')
        })(o),
    e.theme !== n.theme && ($(o, 'remove', e.theme), $(o, 'add', n.theme))
}
var nt = 1,
  it = []
function rt(n, i) {
  var o,
    a,
    u,
    f,
    m,
    v = _(n, i)
  if (!v.multiple && n._tippy) return null
  var h,
    b,
    y,
    g = !1,
    w = v.placement,
    A = !1,
    T = !1,
    E = [],
    C = j(ct, v.interactiveDebounce),
    I = nt++,
    O = tt(I, v),
    k = X(O),
    D = k.tooltip,
    M = k.content,
    S = {
      id: I,
      reference: n,
      popper: O,
      popperChildren: k,
      popperInstance: null,
      props: v,
      state: {
        isEnabled: !0,
        isVisible: !1,
        isDestroyed: !1,
        isMounted: !1,
        isShown: !1,
      },
      clearDelayTimeouts: function() {
        clearTimeout(u), clearTimeout(f), cancelAnimationFrame(m)
      },
      setProps: function(t) {
        if (S.state.isDestroyed) return
        st()
        var i = S.props,
          r = _(n, e({}, S.props, t, { ignoreAttributes: !0 }))
        ;(r.ignoreAttributes = x(t, 'ignoreAttributes')
          ? t.ignoreAttributes || !1
          : i.ignoreAttributes),
          (S.props = r),
          at(),
          Y(),
          (C = j(ct, r.interactiveDebounce)),
          et(O, i, r, S.state.isVisible),
          (S.popperChildren = X(O)),
          S.popperInstance &&
            (p.some(function(e) {
              return x(t, e) && t[e] !== i[e]
            })
              ? (S.popperInstance.destroy(),
                mt(),
                S.state.isVisible && S.popperInstance.enableEventListeners(),
                S.props.followCursor && a && pt(a))
              : S.popperInstance.update())
      },
      setContent: function(t) {
        S.setProps({ content: t })
      },
      show: function() {
        var t =
            arguments.length > 0 && void 0 !== arguments[0]
              ? arguments[0]
              : V(S.props.duration, 0, s.duration[1]),
          e =
            !(arguments.length > 1 && void 0 !== arguments[1]) || arguments[1],
          i = S.state.isVisible,
          o = S.state.isDestroyed,
          p = !S.state.isEnabled,
          u = L.isTouch && !S.props.touch
        if (i || o || p || u) return
        if (J().hasAttribute('disabled')) return
        if (!1 === S.props.onShow(S)) return
        K(), (O.style.visibility = 'visible'), (S.state.isVisible = !0)
        var c = W()
        B(e ? c.concat(O) : c, 0),
          (h = function() {
            S.state.isVisible &&
              (z() || S.popperInstance.update(),
              S.popperChildren.backdrop &&
                (S.popperChildren.content.style.transitionDelay =
                  Math.round(t / 12) + 'ms'),
              S.props.sticky &&
                (function() {
                  B([O], r ? 0 : S.props.updateDuration),
                    (function t() {
                      S.popperInstance.scheduleUpdate()
                      S.state.isMounted ? requestAnimationFrame(t) : B([O], 0)
                    })()
                })(),
              B([O], S.props.updateDuration),
              B(c, t),
              P(c, 'visible'),
              (function(t, e) {
                rt(t, e)
              })(t, function() {
                S.props.aria &&
                  J().setAttribute('aria-'.concat(S.props.aria), D.id),
                  S.props.onShown(S),
                  (S.state.isShown = !0)
              }))
          }),
          (function() {
            A = !1
            var t = z()
            S.popperInstance
              ? (t ||
                  (S.popperInstance.scheduleUpdate(),
                  S.popperInstance.enableEventListeners()),
                F(S.popperInstance.modifiers, S.props.flip))
              : (mt(), t || S.popperInstance.enableEventListeners())
            ;(S.popperInstance.reference = n),
              t &&
                a &&
                requestAnimationFrame(function() {
                  pt(a)
                })
            var e = S.props.appendTo,
              i = 'parent' === e ? n.parentNode : q(e, [n])
            i.contains(O) ||
              (i.appendChild(O), S.props.onMount(S), (S.state.isMounted = !0))
          })()
      },
      hide: function() {
        var t =
            arguments.length > 0 && void 0 !== arguments[0]
              ? arguments[0]
              : V(S.props.duration, 1, s.duration[1]),
          e = !S.state.isMounted,
          n = S.state.isDestroyed,
          i = !S.state.isEnabled
        if (e || n || i) return
        if (!1 === S.props.onHide(S)) return
        $(),
          (O.style.visibility = 'hidden'),
          (S.state.isVisible = !1),
          (S.state.isShown = !1)
        var r = W()
        B(r, t),
          P(r, 'hidden'),
          (function(t, e) {
            rt(t, function() {
              !S.state.isVisible &&
                O.parentNode &&
                O.parentNode.contains(O) &&
                e()
            })
          })(t, function() {
            g || R(),
              S.props.aria && J().removeAttribute('aria-'.concat(S.props.aria)),
              S.popperInstance.disableEventListeners(),
              (S.popperInstance.options.placement = S.props.placement),
              O.parentNode.removeChild(O),
              S.props.onHidden(S),
              (S.state.isMounted = !1)
          })
      },
      enable: function() {
        S.state.isEnabled = !0
      },
      disable: function() {
        S.state.isEnabled = !1
      },
      destroy: function() {
        if (S.state.isDestroyed) return
        S.enable(),
          S.hide(0),
          st(),
          delete n._tippy,
          S.popperInstance && S.popperInstance.destroy()
        S.state.isDestroyed = !0
      },
    }
  return (
    (n._tippy = S),
    (O._tippy = S),
    at(),
    v.lazy || mt(),
    v.showOnInit && ht(),
    O.addEventListener('mouseenter', function() {
      S.props.interactive &&
        S.state.isVisible &&
        'mouseenter' === o &&
        S.clearDelayTimeouts()
    }),
    O.addEventListener('mouseleave', function() {
      S.props.interactive &&
        'mouseenter' === o &&
        document.addEventListener('mousemove', C)
    }),
    v.onCreate(S),
    S
  )
  function U() {
    return N(['top', 'bottom'], Z(w))
  }
  function z() {
    return S.props.followCursor && 'focus' !== o
  }
  function W() {
    return [D, M, S.popperChildren.backdrop]
  }
  function R() {
    document.removeEventListener('mousemove', pt)
  }
  function Y() {
    document.body.removeEventListener('mouseleave', bt),
      document.removeEventListener('mousemove', C),
      (it = it.filter(function(t) {
        return t !== C
      }))
  }
  function J() {
    return S.props.triggerTarget || n
  }
  function G(t) {
    if (!S.props.interactive || !O.contains(t.target)) {
      if (J().contains(t.target)) {
        if (L.isTouch) return
        if (S.state.isVisible && N(S.props.trigger, 'click')) return
      }
      !0 === S.props.hideOnClick &&
        (S.clearDelayTimeouts(),
        S.hide(),
        (T = !0),
        setTimeout(function() {
          T = !1
        }),
        S.state.isMounted || $())
    }
  }
  function K() {
    document.addEventListener('mousedown', G, !0)
  }
  function $() {
    document.removeEventListener('mousedown', G, !0)
  }
  function rt(t, e) {
    function n(t) {
      t.target === D && (Q(D, 'remove', n), e())
    }
    if (0 === t) return e()
    Q(D, 'remove', b), Q(D, 'add', n), (b = n)
  }
  function ot(t, e) {
    var n = arguments.length > 2 && void 0 !== arguments[2] && arguments[2]
    J().addEventListener(t, e, n),
      E.push({ eventType: t, handler: e, options: n })
  }
  function at() {
    S.props.touchHold && (ot('touchstart', ut, d), ot('touchend', dt, d)),
      N(S.props.trigger, 'click') ||
        ot('click', function() {
          L.isTouch || !0 !== S.props.hideOnClick || S.hide()
        }),
      S.props.trigger
        .trim()
        .split(' ')
        .forEach(function(t) {
          if ('manual' !== t)
            switch ((ot(t, ut), t)) {
              case 'mouseenter':
                ot('mouseleave', dt)
                break
              case 'focus':
                ot(r ? 'focusout' : 'blur', lt)
            }
        })
  }
  function st() {
    E.forEach(function(t) {
      var e = t.eventType,
        n = t.handler,
        i = t.options
      J().removeEventListener(e, n, i)
    }),
      (E = [])
  }
  function pt(t) {
    var e = (a = t),
      i = e.clientX,
      r = e.clientY
    if (y) {
      var o = c(t.target, function(t) {
          return t === n
        }),
        s = n.getBoundingClientRect(),
        p = S.props.followCursor,
        u = 'horizontal' === p,
        d = 'vertical' === p,
        l = U(),
        f = l ? 0 : 50,
        m = l ? 50 : 0
      ;(!o && S.props.interactive) ||
        ((S.popperInstance.reference = {
          clientWidth: 0,
          clientHeight: 0,
          getBoundingClientRect: function() {
            return {
              width: l ? 100 : 0,
              height: l ? 0 : 100,
              top: (u ? s.top : r) - f,
              bottom: (u ? s.bottom : r) + f,
              left: (d ? s.left : i) - m,
              right: (d ? s.right : i) + m,
            }
          },
        }),
        S.popperInstance.scheduleUpdate()),
        (L.isTouch || ('initial' === p && S.state.isVisible)) && R()
    }
  }
  function ut(t) {
    T ||
      !S.state.isEnabled ||
      ft(t) ||
      (S.state.isVisible ||
        ((o = t.type),
        t instanceof MouseEvent &&
          ((a = t),
          it.forEach(function(e) {
            return e(t)
          }))),
      'click' === t.type && !1 !== S.props.hideOnClick && S.state.isVisible
        ? bt(t)
        : ht(t))
  }
  function ct(t) {
    c(t.target, function(t) {
      return t === n || t === O
    }) ||
      ((function(t, e, n, i) {
        if (!t) return !0
        var r = n.clientX,
          o = n.clientY,
          a = i.interactiveBorder,
          s = i.distance,
          p = e.top - o > ('top' === t ? a + s : a),
          u = o - e.bottom > ('bottom' === t ? a + s : a),
          c = e.left - r > ('left' === t ? a + s : a),
          d = r - e.right > ('right' === t ? a + s : a)
        return p || u || c || d
      })(Z(w), O.getBoundingClientRect(), t, S.props) &&
        (Y(), bt(t)))
  }
  function dt(t) {
    if (!ft(t))
      return S.props.interactive
        ? (document.body.addEventListener('mouseleave', bt),
          document.addEventListener('mousemove', C),
          void it.push(C))
        : void bt(t)
  }
  function lt(t) {
    t.target === J() &&
      ((S.props.interactive &&
        t.relatedTarget &&
        O.contains(t.relatedTarget)) ||
        bt(t))
  }
  function ft(t) {
    var e = 'ontouchstart' in window,
      n = N(t.type, 'touch'),
      i = S.props.touchHold
    return (e && L.isTouch && i && !n) || (L.isTouch && !i && n)
  }
  function mt() {
    var i = S.props.popperOptions,
      r = S.popperChildren.arrow,
      o = H(i, 'preventOverflow')
    function a(t) {
      var n = w
      ;(w = t.placement),
        S.props.flip &&
          !S.props.flipOnUpdate &&
          (t.flipped && (S.popperInstance.options.placement = t.placement),
          F(S.popperInstance.modifiers, !1)),
        D.setAttribute('data-placement', w),
        !1 !== t.attributes['x-out-of-boundaries']
          ? D.setAttribute('data-out-of-boundaries', '')
          : D.removeAttribute('data-out-of-boundaries')
      var i = Z(w),
        r = D.style
      ;(r.top = '0'),
        (r.left = '0'),
        (r[U() ? 'top' : 'left'] = ''.concat(
          (N(['bottom', 'right'], Z(w)) ? 1 : -1) * S.props.distance,
          'px',
        ))
      var a = o && void 0 !== o.padding ? o.padding : l,
        s = 'number' == typeof a,
        p = e(
          {
            top: s ? a : a.top,
            bottom: s ? a : a.bottom,
            left: s ? a : a.left,
            right: s ? a : a.right,
          },
          !s && a,
        )
      ;(p[i] = s ? a + S.props.distance : (a[i] || 0) + S.props.distance),
        (S.popperInstance.modifiers.filter(function(t) {
          return 'preventOverflow' === t.name
        })[0].padding = p),
        (y = p),
        w !== n && S.popperInstance.update()
    }
    var s = e({ eventsEnabled: !1, placement: S.props.placement }, i, {
      modifiers: e({}, i ? i.modifiers : {}, {
        preventOverflow: e(
          { boundariesElement: S.props.boundary, padding: l },
          o,
        ),
        arrow: e({ element: r, enabled: !!r }, H(i, 'arrow')),
        flip: e(
          {
            enabled: S.props.flip,
            padding: S.props.distance + l,
            behavior: S.props.flipBehavior,
          },
          H(i, 'flip'),
        ),
        offset: e({ offset: S.props.offset }, H(i, 'offset')),
      }),
      onCreate: function(t) {
        a(t), vt(), i && i.onCreate && i.onCreate(t)
      },
      onUpdate: function(t) {
        a(t), vt(), i && i.onUpdate && i.onUpdate(t)
      },
    })
    S.popperInstance = new t(n, O, s)
  }
  function vt() {
    !A &&
      h &&
      ((A = !0),
      (function(t) {
        t.offsetHeight
      })(O),
      h())
  }
  function ht(t) {
    if (
      (S.clearDelayTimeouts(), t && S.props.onTrigger(S, t), !S.state.isVisible)
    ) {
      if (((g = !0), S.props.wait)) return S.props.wait(S, t)
      z() &&
        !S.state.isMounted &&
        (S.popperInstance || mt(), document.addEventListener('mousemove', pt)),
        K()
      var e = V(S.props.delay, 0, s.delay)
      e
        ? (u = setTimeout(function() {
            S.show()
          }, e))
        : S.show()
    }
  }
  function bt(t) {
    if ((S.clearDelayTimeouts(), S.props.onUntrigger(S, t), !S.state.isVisible))
      return R()
    g = !1
    var e = V(S.props.delay, 1, s.delay)
    e
      ? (f = setTimeout(function() {
          S.state.isVisible && S.hide()
        }, e))
      : (m = requestAnimationFrame(function() {
          S.hide()
        }))
  }
}
function ot(t, n) {
  document.addEventListener('touchstart', k, d),
    window.addEventListener('blur', M)
  var i = e({}, s, n),
    r = (function(t) {
      if (U(t)) return [t]
      if (t instanceof NodeList) return u(t)
      if (Array.isArray(t)) return t
      try {
        return u(document.querySelectorAll(t))
      } catch (t) {
        return []
      }
    })(t).reduce(function(t, e) {
      var n = e && rt(e, i)
      return n && t.push(n), t
    }, [])
  return U(t) ? r[0] : r
}
;(ot.version = '5.0.0-alpha.0'),
  (ot.defaultProps = s),
  (ot.currentInput = L),
  (ot.setDefaultProps = function(t) {
    Object.keys(t).forEach(function(e) {
      s[e] = t[e]
    })
  }),
  (ot.hideAll = function() {
    var t = arguments.length > 0 && void 0 !== arguments[0] ? arguments[0] : {},
      e = t.exclude,
      n = t.duration
    u(document.querySelectorAll(w)).forEach(function(t) {
      var i,
        r = t._tippy
      if (r) {
        var o = !1
        e &&
          (o =
            (i = e) && i._tippy && !i.classList.contains(m)
              ? r.reference === e
              : t === e.popper),
          o || r.hide(n)
      }
    })
  }),
  n &&
    setTimeout(function() {
      u(document.querySelectorAll('[data-tippy]')).forEach(function(t) {
        var e = t.getAttribute('data-tippy')
        e && ot(t, { content: e })
      })
    })
export { ot as a, n as b, e as c, V as d }
//# sourceMappingURL=tippy.chunk.min.js.map
